apiVersion: v1
kind: Service
metadata:
  name: go-service
spec:
  ports:
    - name: http
      port: 8888
      protocol: TCP
      targetPort: 8888
  selector:
    run: go-sample
  sessionAffinity: None
  type: ClusterIP
